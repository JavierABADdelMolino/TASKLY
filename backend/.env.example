# Configuración de entorno para Taskly (Backend)

# --- Entorno general ---
# Modo de ejecución: development, production, etc.
NODE_ENV=development

# Puerto en el que se ejecuta el servidor (opcional, default 4000)
PORT=4000

# --- Base de datos MongoDB Atlas ---
# URI de conexión principal a MongoDB Atlas (incluye nombre de BD)
MONGODB_URI=
# URI de respaldo o fallback
DATABASE_URL=

# --- Seguridad y autenticación ---
# Secreto para firmar y verificar JSON Web Tokens (JWT)
JWT_SECRET=
# Origen permitido del cliente (para CORS)
CLIENT_URL=
# ID del cliente de Google para autenticación OAuth2
REACT_APP_GOOGLE_CLIENT_ID=

# --- OpenAI (para integración con GPT en el backend) ---
OPENAI_API_KEY=

# --- Resend (envío de emails) ---
# Clave API de Resend (https://resend.com)
RESEND_API_KEY=
# Dirección 'from' para tus correos
EMAIL_FROM=
# Dirección de correo de soporte
SUPPORT_EMAIL=

# --- Cloudinary (subida de avatares en producción) ---
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
